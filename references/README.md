# References

The system of record might be bibtex files, or a CSV file,
or possibly an SQLite3 database file. Not sure. SQLite3 is
compelling as it can be exported as a dump and archived in
the repo or on S3.

## Kindle notes

Kindle provides an HTML export which is not particularly
convenient. The `kindle_notes` directory collects the raw,
unprocessed Kindle output.
